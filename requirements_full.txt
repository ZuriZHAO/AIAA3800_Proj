# EmotiCompanion 集成环境总清单 —— 一次装齐框架 + 四个模块的依赖
# 在 3800 环境（python=3.10）里：
#   pip install -r requirements_full.txt
# 各模块依赖的详细说明见 requirements_m1..m4.txt。
#
# ⚠️ 两个不能用 pip 装 / 需特别处理的依赖：
#   1) torch / torchaudio：按你的 CUDA 版本从 https://pytorch.org 单独装（本机有 GPU，
#      不要让本文件把它覆盖成 CPU 版），所以这里刻意不列 torch。
#   2) ffmpeg：是系统级程序不是 pip 包，audio_extract.py 抽音频要用。用 conda 装：
#         conda install -n 3800 -c conda-forge ffmpeg

# ---- 框架 (app.py) ----
gradio>=4.39
numpy

# ---- M1 · 人脸情绪① + GradCAM⑦ ----
# ⚠️ timm 必须 0.9.16：hsemotion 权重是 timm 0.9.x 序列化的整模型 pickle
timm==0.9.16
hsemotion
opencv-python

# ---- M2 · 语音情绪③（Qwen-Omni via OpenAI 兼容 API）----
openai>=1.0.0
python-dotenv>=1.0.0

# ---- M3 · LLM 推理⑤（DeepSeek/OpenAI）+ 音乐生成⑥（MusicGen）----
transformers>=4.31.0
accelerate
scipy
soundfile

# ---- M4 · 疲劳检测② ----
# 可选增强：装上有精确 EAR/MAR 关键点；不装则 fatigue.py 自动退回 OpenCV Haar 粗判。
# fatigue.py 已用 mediapipe 的 Tasks API（FaceLandmarker），需要模型文件：
#     models/face_landmarker.task
# 下载地址（约 3.7MB，放到项目根的 models/ 下）：
#   https://storage.googleapis.com/mediapipe-models/face_landmarker/face_landmarker/float16/latest/face_landmarker.task
# 模型缺失或 mediapipe 未装时，fatigue.py 自动退回 Haar 粗判，不阻塞。
mediapipe
